# 소품지도 작업 규칙

## 프로젝트 개요

소품지도는 취향에 맞는 소품샵, 가챠샵, 굿즈샵을 발견하고 저장하며 방문 기록과 코스를 관리하는 모바일 웹 서비스다.

이 저장소는 `pnpm` 워크스페이스로 구성한다.

- `frontend/`: Next.js 기반 사용자 화면
- `backend/`: Express와 MongoDB 기반 API 서버
- `packages/`: 프론트엔드와 백엔드가 함께 사용하는 타입과 상수

현재 구조와 구현 상태는 `MEMORY.md`를 참고한다. 단, `MEMORY.md`는 기록 시점의 스냅샷이므로 실제 코드와 Git 상태를 우선한다.

## 작업 시작 절차

1. `git status --short --branch`로 현재 브랜치와 사용자 변경을 확인한다.
2. 진행 중인 브랜치를 이어갈 때는 `git worktree list`와 해당 워크트리의 상태도 확인한다.
3. 수정할 파일과 관련 타입, 테스트, 비슷한 구현 예시를 먼저 읽는다.
4. UI 작업은 이슈의 완료 조건과 현재 코드를 확인한다. `docs/superpowers/`가 현재 브랜치에 있을 때만 관련 설계·계획을 함께 확인한다.
5. 요구사항과 기존 코드가 충돌하면 임의로 결정하지 말고 충돌 내용을 알린다.
6. 요청 범위 밖의 리팩터링과 파일 정리는 하지 않는다.

## 코드 작성 규칙

- TypeScript의 `strict` 설정을 유지하고 불필요한 `any`와 타입 단언을 피한다.
- 프론트엔드와 백엔드가 공유하는 API 계약은 `@sopum-map/shared`에 정의한다.
- 함수는 한 가지 역할만 맡도록 작게 유지한다.
- 상위 함수와 컴포넌트에는 작업의 목적과 흐름을 남기고, 인증 확인·데이터 변환·확인창 제어 같은 구현 상세는 이름 있는 함수, 훅, 컴포넌트 또는 서비스로 분리한다.
- 분리는 줄 수가 아니라 읽는 사람이 동시에 알아야 하는 맥락의 수를 기준으로 한다. 함께 읽고 수정하는 상태·동작·UI는 가까이 두고, 서로 독립적으로 바뀌는 책임은 경계 밖으로 옮긴다.
- 추상화한 단위의 이름과 입력·출력만으로 역할을 이해할 수 있게 한다. 내부 구현을 알아야 사용할 수 있다면 경계를 다시 정리한다.
- 한 번만 쓰이는 짧고 명확한 로직까지 기계적으로 분리하지 않는다. 분리로 숨겨지는 비용보다 맥락 감소와 변경 격리 효과가 클 때 추상화한다.
- 함수를 작성할 때 함수 바로 위에 어떤 함수인지 설명하는 짧은 주석을 단다.
- 주석은 코드 내용을 그대로 읽어주는 대신 함수의 목적이나 중요한 이유를 설명한다.
- 이름은 현재 코드의 규칙을 따르고 파일명은 `kebab-case`를 우선한다.
- 사용자 입력은 API 경계에서 검증하고 예상 가능한 오류는 공통 오류 형식으로 반환한다.
- 비밀값, 토큰, `.env` 파일을 커밋하지 않는다.

## 변경하기 쉬운 코드

- 코드 품질은 모양이나 추상화의 개수가 아니라 요구사항이 바뀔 때 안전하게 수정할 수 있는지로 판단한다.
- 가독성, 예측 가능성, 응집도, 결합도는 서로 충돌할 수 있다. 함께 수정하지 않으면 버그가 나는 코드는 응집도를 우선하고, 변경 방향이 갈릴 가능성이 큰 코드는 낮은 결합도와 명확한 중복을 우선한다.

### 가독성

- 함수, 클래스와 컴포넌트가 동시에 다루는 맥락이 많아지면 역할을 나눈다. 분기상 함께 실행되지 않는 흐름은 분기 지점을 한곳에 모으고 각 흐름을 별도 단위로 분리한다.
- 인증 확인, 권한 판단, 데이터 변환처럼 상위 코드의 목적을 가리는 구현 상세는 역할이 드러나는 이름의 함수, 서비스, 훅 또는 하위 컴포넌트로 감춘다.
- 복잡한 조건식은 조건의 의미가 드러나는 이름을 붙인다. 숫자와 문자열이 정책이나 동작 시점을 나타내면 의미 있는 상수로 선언한다.
- 코드는 위에서 아래로 읽히게 작성한다. 중첩 삼항 연산자를 사용하지 않고, 여러 조건이 겹치면 이름 있는 함수와 `if` 문으로 풀어 쓴다.
- 비교식은 판단할 대상을 왼쪽에 둔다. 예를 들어 `price > MAX_PRICE`처럼 읽는 순서와 의미가 일치하게 작성한다.
- 작은 로직을 무조건 다른 파일로 옮기지 않는다. 추출 때문에 파일과 함수를 반복해서 오가야 한다면 가까운 위치에 둔다.

### 예측 가능성

- 이름, 매개변수와 반환 타입만 보고 주요 동작을 예상할 수 있게 한다.
- 기본 라이브러리나 기존 함수와 이름은 같지만 추가 동작이 있는 래퍼를 만들지 않는다. 인증 정보 추가처럼 동작이 달라지면 차이가 드러나는 이름을 사용한다.
- 같은 계열의 API 함수, 훅과 검증 함수는 같은 형태로 반환한다. 일부는 전체 결과 객체를 반환하고 일부는 값만 반환하는 식으로 섞지 않는다.
- 함수 이름이 약속하지 않은 저장, 로깅, 화면 이동과 상태 변경을 숨기지 않는다. 부수 효과가 필요하면 이름에 드러내거나 호출하는 쪽에서 명시적으로 실행한다.

### 응집도

- 함께 바뀌는 코드, 타입, 상수와 테스트는 같은 기능 또는 도메인 경계 가까이에 둔다. 파일 종류만을 기준으로 전역 폴더에 흩어 놓지 않는다.
- 여러 코드가 같은 정책값에 의존하면 하나의 상수를 공유한다. 우연히 값만 같은 경우에는 성급하게 묶지 않는다.
- 검증 규칙은 변경 단위를 기준으로 배치한다. 독립적으로 재사용되는 입력은 해당 입력 가까이에 두고, 함께 바뀌는 요청이나 폼 규칙은 하나의 스키마로 관리한다.

### 결합도와 추상화

- 함수, 클래스, 서비스와 훅은 하나의 관심사만 다룬다. 필요한 기능 하나를 쓰기 위해 관련 없는 상태와 동작까지 의존하게 만들지 않는다.
- 두 곳에서 사용된다는 이유만으로 바로 공통화하지 않는다. 요구사항이 서로 달라질 가능성이 있으면 명확한 중복을 유지하고, 함께 바뀐다는 근거가 생겼을 때 추상화한다.
- 추상화의 목적은 재사용 자체가 아니라 변경 비용을 줄이는 데 있다. 매개변수, Props와 분기가 계속 늘어나면 공통 추상화를 유지하기보다 책임을 다시 나눈다.
- 중간 계층이 사용하지 않는 값을 전달만 하지 않게 한다. 전달 경로가 길어지면 각 소비자가 필요한 값을 직접 받도록 경계를 다시 설계한다.
- 상태 변수, 외부 자원 생성·해제, 다단계 전환과 같은 반복 제어 흐름은 안정된 인터페이스 안에 감출 수 있다. 단, 이를 위해 새 라이브러리를 바로 추가하지 말고 현재 프로젝트의 도구로 충분한지 먼저 확인한다.

### 리뷰 기준

- 한 단위를 이해하려고 너무 많은 상태, 조건과 파일을 동시에 추적해야 하지 않는지 확인한다.
- 이름 없는 복잡한 조건, 매직 넘버, 중첩 삼항과 숨은 부수 효과가 없는지 확인한다.
- 같은 계열 함수와 모듈의 이름, 입력과 반환 형태가 일관적인지 확인한다.
- 같이 바뀌는 코드가 가까이 있고, 독립적으로 바뀌는 코드가 불필요하게 묶이지 않았는지 확인한다.
- 공통 인터페이스의 매개변수나 Props가 사용처별 예외를 수용하느라 계속 늘어나지 않는지 확인한다.
- 추상화를 추가하거나 유지할 때 재사용 횟수보다 실제 변경 비용이 줄어드는지 확인한다.

## 라이브러리 버전과 문법

- 문법과 API를 선택하기 전에 작업 대상 패키지의 `package.json`, `pnpm-lock.yaml`, `tsconfig.json`에서 실제 설치 버전과 컴파일 설정을 확인한다. `MEMORY.md`나 기억 속 최신 문법만으로 결정하지 않는다.
- 현재 설치된 버전의 공식 문서, 패키지에 포함된 타입 선언과 기존 사용 예시를 우선한다. 공식 문서의 최신 예시가 설치 버전과 다르면 설치 버전에 맞는 문법을 사용하고 차이를 알린다.
- 폐기 예정인 API와 호환성용 옛 문법은 새 코드에 사용하지 않는다. 기존 코드에서 발견하면 요청 범위에 직접 포함될 때만 함께 고친다.
- 설치되지 않은 라이브러리나 플러그인의 API를 있다고 가정하지 않는다. 새 의존성이 꼭 필요하면 기존 도구로 해결할 수 없는 이유와 버전 호환성을 먼저 확인한다.
- 프론트엔드·백엔드·공용 패키지는 TypeScript 6을 사용한다. 각 패키지의 서로 다른 `target`, `module`, `moduleResolution` 설정을 지키고, 공유 코드는 세 패키지 모두가 컴파일할 수 있는 문법과 타입 계약으로 작성한다.

## 프론트엔드 규칙

- `frontend/AGENTS.md`의 Next.js 규칙이 이 문서보다 구체적이므로 프론트엔드 작업에서는 함께 적용한다.
- Next.js 관련 코드를 수정하기 전에 `frontend/node_modules/next/dist/docs/`에서 현재 버전 문서를 확인한다.
- 모바일 우선으로 구현하고 320px 이상의 화면과 `#mobile-app`의 최대 너비 480px를 고려한다.
- 기존 디자인 토큰과 공용 UI 컴포넌트를 우선 사용한다.
- 접근 가능한 이름, 키보드 조작, 포커스 상태를 유지한다.

## 백엔드 규칙

- 요청 흐름은 라우터 → 컨트롤러 → 서비스 → 모델 순서로 분리한다.
- 요청 검증에는 기존 Zod 패턴을 사용한다.
- MongoDB 목록 조회에는 페이지네이션과 적절한 인덱스를 고려한다.
- 같은 종류의 데이터를 항목마다 따로 조회하는 N+1 쿼리를 피한다.
- 데이터베이스 스키마나 인덱스를 바꿀 때 영향 범위를 먼저 확인한다.

## 주요 명령어

```bash
pnpm install
pnpm --dir frontend dev
pnpm --dir frontend test:unit
pnpm --dir frontend lint
pnpm --dir frontend build
pnpm --dir frontend storybook
pnpm --dir frontend exec vitest --project storybook --run
pnpm --dir backend dev
pnpm --dir backend test
pnpm --dir backend build
pnpm --dir packages build
```

## 완료 기준

- 변경한 영역의 테스트, 빌드, 린트를 실행한다.
- UI 변경은 320px, 390px, 480px 화면과 키보드·포커스 동작을 확인한다.
- 실행하지 못한 검증과 그 이유를 명확히 알린다.
- `git diff --check`로 공백 오류를 확인한다.
- 변경 파일 목록을 검토해 요청과 관계없는 파일이 포함되지 않았는지 확인한다.
- 패키지 구조, 핵심 기술, 공용 계약 경계처럼 오래 유지되는 프로젝트 맥락이 바뀌었다면 `MEMORY.md`를 갱신한다. 개별 기능·UI·이슈·PR 상태 변경에는 갱신하지 않는다.

## 사용자 부재 중 자율 작업

- 사용자가 일정 시간 자리를 비우며 질문 없이 진행해 달라고 명시하면, 이미 승인된 설계·피그마·이슈 완료 조건·기존 코드 규칙 안에서 가장 추천하는 방식을 선택해 계속 작업한다.
- 발견 가능한 정보로 안전하게 결정할 수 있는 내용은 질문 때문에 멈추지 않는다. 결정 근거와 트레이드오프는 PR 또는 작업 결과에 기록한다.
- 사용자 부재는 파괴적 작업, 비밀값 사용, 권한 확대, 요구 범위 밖의 외부 변경을 허용하지 않는다. 이런 항목이 막히면 다른 안전한 작업을 먼저 진행한다.
- 여러 이슈를 요청받은 경우 이슈별 브랜치와 PR을 분리하고, 각 변경은 관련 검증을 통과한 뒤 올린다.
