import { VisitLogFormScreen } from "./_components/VisitLogFormScreen";

/** 후기 작성 경로가 준비되는 동안 뼈대 화면을 표시합니다. */
export default function VisitLogLoading() {
  return <VisitLogFormScreen shopId="" shopName="" state="loading" />;
}
